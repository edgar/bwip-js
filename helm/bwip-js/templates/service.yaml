apiVersion: v1
kind: Service
metadata:
  name: {{ include "bwip-js.fullname" . }}
  labels:
{{ include "bwip-js.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bwip-js.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
